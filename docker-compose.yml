services:
  frontend:
    image: ghcr.io/<github-user>/bkhouse-frontend:latest
    restart: always
    expose: ["80"]

  backend:
    image: ghcr.io/<github-user>/bkhouse-backend:latest
    restart: always
    env_file: .env
    expose: ["8080"]

  socket:
    image: ghcr.io/<github-user>/bkhouse-socket:latest
    restart: always
    expose: ["3000"]

  nginx:
    image: nginx:1.27-alpine
    restart: always
    ports: ["80:80", "443:443"]
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./certbot/www:/var/www/certbot:ro
      - ./certbot/conf:/etc/letsencrypt:ro
    depends_on: [frontend, backend, socket]

  certbot:
    image: certbot/certbot
    volumes:
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done'"